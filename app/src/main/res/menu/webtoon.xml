<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/overflow_share"
        app:showAsAction="never"
        android:title="공유하기"/>

    <item android:id="@+id/overflow_tmpsave"
        app:showAsAction="never"
        android:title="임시저장"/>

    <item android:id="@+id/overflow_save"
        app:showAsAction="never"
        android:title="소장하기"/>

    <item android:id="@+id/overflow_about"
        app:showAsAction="never"
        android:title="유료보기 이용안내"/>

</menu>